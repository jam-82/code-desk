document.addEventListener('DOMContentLoaded', () => {

    const grid = document.querySelector('.grid')

    var square = document.createElement('div')
    
    grid.appendChild(square)




})

// we get the div with the class of grid using our querySelector() and call it grid
// using createElement we create a div and call it square
// then we put this created div into our grid using appendChild()