// .forEach
// syntax: array.forEach(x => x)

// const stored = document.querySelectorAll('.container-flo')
// console.log(stored)

// stored.forEach(circle => circle.style.backgroundColor = "hotpink")

// simple example

// const names = ['Ania', 'Bobby', 'Melanie']
// names.forEach(name => console.log("I am " + name))

const scores = [32, 543, 43, 43, 22, 22, 44,22]

scores.forEach(score => {
    console.log(score + 8)
    }
)